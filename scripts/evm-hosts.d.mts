// Types for scripts/evm-hosts.mjs (imported by vite.config.ts and hosts.test.ts).
export function evmGatewayUrl(root: string): string;
export function evmHostPermissions(root: string): string[];
export function allEvmHostPatterns(root: string): string[];
