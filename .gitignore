node_modules/
dist/
release/
*.log
.DS_Store
/store/
# local-only docs (owner's decision: not published to the public repo)
/docs/

# Local secrets (never committed): API keys injected into DEV builds only.
*.local.json
